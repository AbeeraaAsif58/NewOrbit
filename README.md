# Orbit Starter (React + Vite + Tailwind + GSAP + React Router)

Run:
```
npm install
npm run dev
```
Pages: Home, Services, Projects, Gallery, About, Contact. Navbar with active links and small GSAP animation.
"# OrbitWalls" 
